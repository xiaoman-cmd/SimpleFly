# SimpleFly 使用手册

> 小鹤双拼 / 小鹤音形输入法 · macOS 本机自用版（当前 0.7.0）
> 开发者向的实现笔记见同目录 `README.md`，本文只讲「怎么装、怎么用、换台机器怎么搬」。

---

## 一、这是什么

| | 说明 |
|---|---|
| ✅ 能打 | 小鹤双拼（2 键/字）、小鹤音形（4 码定字、四键唯一即上屏）、自定义短语、标点、查编码（拼音反查音形码） |
| ✅ 自用 | 只装在自己这台 Mac；ad-hoc 签名，**换台机器会被 Gatekeeper 拦**，不能直接拷给别人用 |
| ❌ 不做 | 整句输入、用户词库/自学习、简繁转换、鼠标点选候选、翻页（候选一页放得下） |

为什么不做整句：小鹤音形官方方案本身就关掉了整句（`enable_sentence: false`），它的输入码是「双拼 + 双形」，本版与官方口径一致。

---

## 二、安装与激活

### 1. 构建并安装

```bash
cd /path/to/simplefly

./build.sh          # 编译 + 组装 .app + 签名（几秒）
./install.sh        # 装到 ~/Library/Input Methods/SimpleFly.app（不要 sudo）
```

> 装之前若想确认码表读得到：`build/SimpleFly.app/Contents/MacOS/SimpleFly --check`
>
> ⚠️ **本仓库不含码表**：新机器第一次构建前，先照 §八 生成 `simplefly.dict`，否则装出来打不了字（有明确提示，不是坏了）。

### 2. 在系统里启用

1. **注销并重新登录**（输入法列表只在登录时刷新，首次不出现是常态）。
2. 系统设置 › 键盘 › 文字输入 › 输入法 › 编辑… › `+` › 中文（简体） › 选中 **SimpleFly**。
   - 列表里显示 **「SimpleFly」**（中英文环境同名），图标是汉字「飞」。想换成别的字：改 `tools/make_icon.m` 里默认 `.text`，或 `build/make_icon -t 字` 直接生成。
3. macOS 13+ 首次添加会弹「TextInputMenuAgent 想启用第三方输入法」，**必须点允许**。
### 4. 卸载

```bash
./uninstall.sh
```

---

## 三、日常输入

### 小鹤双拼
每个字固定 **2 键**（声母键 + 韵母键）。敲 `a`–`z`、`;`、`'` 累积编码，实时出候选。

### 小鹤音形
每字 **双拼 2 码 + 形码 2 码 = 4 码**定字：
- 第 4 码落下时若精确匹配**唯一**，直接上屏（不用看候选）。
- 有重码时列出候选，自己选。

### 选词与上屏

| 操作 | 效果 |
|---|---|
| `1`–`9` | 上屏第 N 个候选 |
| `空格` / `回车` | 上屏**当前高亮**的候选（默认第一个） |
| `↑` `↓` `←` `→` | 移动高亮，**四个键都算一维移动**（↑← 往前、↓→ 往后，两端回绕）。没在组字时照常放行给应用移光标 |
| `;`（有候选时） | 上屏**第二个**候选 |
| `,` `.` | **顶屏**：先把当前高亮顶上去，再打标点 |
| `Backspace` | 删掉编码最后一位 |
| `Esc` / `Tab` | 放弃当前编码（Tab 之后照常把 Tab 送给应用） |
| 大写字母 | 原样送出（不进编码缓冲，保留大小写） |

### 标点
标点键走标点表：单候选直接上屏，多候选弹窗，引号 `""` `''` **左右交替**。常用映射：`,`→`，`、`、` `→`。、`^`→`……`、`_`→`——`；详见 §七配置里的「中英标点」开关。

### 符号、特殊字符、Emoji

码表里除了汉字，还塞了四类能直接打出来的非汉字内容 —— 完整清单在 **`docs/符号速查.md`**
（换码表后跑 `./build.sh --symbols` 本地生成；属码表衍生内容，这个文件不进仓库）。这里只说入口：

| 想打什么 | 怎么打 | 规模 |
|---|---|---|
| **快符** | `;` + 一个字母，**打满两码自动上屏**，不用按空格 | 24 个（`;a`→`！`、`;v`→`——`、`;x`→`→`） |
| **特殊符号** | 先打 `of`，候选窗列出分类标签，标签末位那个字母就是下一键 | 26 组 / 676 个（日文、俄文、希腊、注音、拼音、数学、货币、箭头、制表、上下标…） |
| **Emoji** | `oi` 前缀，往后加字母细分 | 313 个（`oi`→😊） |
| **微信表情名** | `ow` 前缀 | 113 个（打出来是 `[微笑]` 这种**字面文本**，微信里才会渲染成表情） |

例：想打片假名 → `of` 看到「日大d」→ 按 `d` → `ofrd` 出 89 个片假名。
想打注音 → `ofvy`。想打数学符号 → `ofux`。

⚠️ **单敲 `;` 不会上屏**：它在码表里有 2 个候选（`：` 和 `；`），要按空格选。
这是官方 `auto_select_pattern: ^;.$` 的刻意行为 —— 只匹配码长 2。

---

## 四、快捷键一览

全部组合键收在这一节，按「日常开关 → 查码 → 简繁 → 同步」分组；各项的详细说明在 §五 对应小节。

**模式开关**

| 按键 | 作用 | 备注 |
|---|---|---|
| 单敲 `Shift` | 中 / 英 一键切换 | 不落盘，开机总是从中文开始 |
| `Ctrl` + `.` | 中英标点切换（，。 ↔ ,.） | 会写回配置 |
| `Shift` + `空格` | 全 / 半角切换 | 会写回配置；全角只作用于标点 |

**查码与候选窗**

| 按键 | 作用 | 备注 |
|---|---|---|
| `Ctrl` + `/` | 查编码模式 开 / 关 | 拼音反查音形码，见 §五 |
| `Ctrl` + `Shift` + `/` | 选中文字 → 查它的音形码 | 先选中一个字或词再按；结果浮层显示约 2.5 秒，见 §五「选中查编码」 |
| `Ctrl` + `;` | 候选窗配色主题循环切换 | 8 套：metro → dark → paper → aqua → luna → ink → google → mojave_dark，会写回配置 |
| `Ctrl` + `Shift` + `H` | 候选窗底部编码提示 开 / 关 | 打字时候选文字底下显示高亮候选的音形码；也可点输入法菜单「候选窗编码提示」，见 §五 |
| `Ctrl` + `Shift` + `;` | 清空重码记忆 | 删掉 `freq.txt`，所有重码恢复码表默认顺序，见 §五「重码记忆」 |

**简繁**

| 按键 | 作用 | 备注 |
|---|---|---|
| `Ctrl` + `Shift` + `F` | 选中文字 → 简繁互转 | 原位替换、自动判方向（单字映射），见 §五「简繁互转」 |
| `Ctrl` + `Shift` + `T` | 输出模式 简 / 繁 切换 | 开启后打出来的都自动转繁体；见 §五「直接输出繁体」 |

**WebDAV 同步**

| 按键 | 作用 | 备注 |
|---|---|---|
| `Ctrl` + `Shift` + `U` | 同步到网盘（短语 + 重码记忆） | 需先配置网盘（菜单「网盘配置…」无快捷键），见 §五「WebDAV 同步」 |
| `Ctrl` + `Shift` + `D` | 从网盘恢复 | 菜单栏输入法菜单里的同名项等价 |

> 切换中英、全角、标点时，屏幕会有约 1 秒的浮层提示；输入法菜单图标本身不变（这是已知限制，见 §九）。
> 输入法菜单（点菜单栏输入法名字）除了上面等价的开关项，还有「网盘配置…」和 WebDAV 三个入口，以及顶部的「更新到最新版」，均无快捷键。

### 更新到最新版（0.7.0）

**场景**：不想每次手动开终端跑 `git pull` + `build.sh` + `install.sh` + 注销重登。

点菜单栏输入法名字 → 顶部 **「更新到最新版」**，后台自动完成，结束后弹系统通知。

| | 说明 |
|---|---|
| 开发者（本机有源码仓库） | 走「拉取 + 构建」：`git fetch` + `merge --ff-only` → `build.sh` → `install.sh`。工作树有未提交改动会自动 `stash` 再还原；**本地与远程分叉时直接报错退出，绝不丢你的工作** |
| 普通用户（没装 git / 开发工具） | 走「预编译下载」：直接下载 GitHub Release 里打包好的 `SimpleFly.app.zip` 解压安装，**不需要 git、也不需要 Xcode 命令行工具** |
| 免登刷新 | 安装后自动重注册并启用输入法，**不用注销重登**（极少数情况需把输入法切走再切回） |
| 你的数据 | 短语、码表、网盘凭据、重码记忆、各项设置**一律不动**；更新前只额外做一份时间戳备份，方便回滚 |
| 日志 | 出问题看 `/tmp/simplefly_update.log` |

---

## 五、查编码模式（拼音反查音形码）

**场景**：知道一个字念什么、但不知道怎么拆形码时，用拼音把它打出来、顺带看音形码。

```text
Ctrl+/          进入查编码模式（屏幕提示「查编码：输入拼音」）—— 它是个常驻拼音输入法
hao             输入拼音，也可直接输小鹤双拼码 hc
                候选按 3 行 × 9 列网格列出同音一整族字，每个字后标音形码：
                  1 好 hc·nz   2 号 hck·nz   3 浩 hcd·nz   4 豪 hcw·nz …
                  中点左边是音码、右边是形码
空格            上屏高亮的字；上屏后不退出模式，HUD 短暂显示「好  hc·nz」方便记码
ni              接着再打拼音继续出字（像普通拼音输入法连着打）
Esc / Ctrl+/    退出模式，回到小鹤双拼
```

| 细节 | 说明 |
|---|---|
| 输入什么 | **全拼**（`hao`）和**小鹤双拼码**（`hc`）都认，按「是否合法拼音音节」自动区分，不用手动切 |
| 按前缀匹配 | 同音字各带形码（好=hc、号=hck、浩=hcd、豪=hcw），会列出**所有编码以该读音开头**的候选 |
| 词语查询 | 多音节拼音也行（`nihao`→`nihc`→`你好`），同一读音下**单字排前、词语排后** |
| 翻页 | 候选超过一页（27 个）时右上角显示 `1/N`；方向键移过边界自动翻页 |
| 常驻 | 默认查到字上屏后**不退出**，可连续打；想改回「查一个就退」见 §七 `ReverseModeAutoExit` |
| 与英文模式 | 互斥：进查编码会自动退出英文模式 |

### 候选窗编码提示（0.6.3）

**场景**：一边打字一边记音形码 —— 不用专门进查编码模式，打字时高亮候选的编码就在眼前。

```text
Ctrl+Shift+H    开 / 关（屏幕提示「编码提示：开 / 关」，也可点输入法菜单切换）
nihao           正常打字，候选窗文字底下多一行小字显示高亮候选的音形码
↓ / ↑           移动高亮，底下的编码跟着变（比如「好 hc·nz」→「号 hck·nz」）
空格            上屏，行为不变
```

| 细节 | 说明 |
|---|---|
| 显示内容 | **当前高亮候选**的完整音形码：单字带中点拆分（`hc·nz`，左边音码右边形码），词语显示整词编码 |
| 即时生效 | 开关改完**下一个候选窗**就生效，不用重启输入法 |
| 自动隐藏 | 标点候选没有编码，该行不出现 |
| 默认状态 | 关。想常开用 `defaults write $D CodeHint -bool YES`（见 §七），或快捷键按一次 |

### 重码记忆（0.5.3）

同一个编码有多个候选时，输入法记住你**上次选了哪个**，下次把它排到第一位——
直接按空格就是上次选的那个，不用再按方向键。

- 只在**码长 ≥ 3 且有重码**时生效，2 码的二简组顺序永远不变（练码手感优先）
- 记忆存在 `~/Library/Application Support/SimpleFly/freq.txt`，一行一条、可手改，**删一行 = 忘一条**
- 30 天没用过的条目自动清掉；`Ctrl+Shift+;` 一键全部清空
- 不想要这个功能：`defaults write com.simplefly.inputmethod.SimpleFly FreqMemory -bool NO`

### 选中查编码（0.5.3）

看到屏幕上有个字想打、又不会拆？先**选中**它，再按 `Ctrl+Shift+/`：

```text
好  hc·nz
```

浮层显示字和音形码（中点分音/形），约 2.5 秒自动消失。词也可以（`你好 nihc`）；
没选中时提示先选中；码表里没有的字会明说「不在码表里」。

### 打错日志 → 短语建议（0.5.4，默认关）

总有几个码天天打、天天不顺？打开 `LogMisses`，输入法记下「打了又整段放弃的码 →
最终上屏了什么」，跑一周后用 `./build.sh --suggest` 生成可以直接贴进 `phrase.txt`
的建议行（已有短语自动排除）。

```bash
defaults write com.simplefly.inputmethod.SimpleFly LogMisses -bool YES
killall SimpleFly          # 打几天字后：
./build.sh --suggest       # 想调阈值：--min 3 / --days 14
```

**隐私**：默认关；日志只落本地（`mislog.tsv`，三列：时间戳/废弃的码/最终上屏的词）；
退一格改错字不算废弃；删掉文件即全部清除。

### 简繁互转（0.5.4 起，0.5.6 双向）

选中一段文字，按 `Ctrl+Shift+F`，原位替换——**自动判方向**：
选中简体就转繁体，选中繁体就转简体，两边都没差异（纯 ASCII / 简繁同形）会提示「未发现简繁差异」：

- **单字映射**（简→繁 3,881 对 + 繁→简 3,221 对，来自 OpenCC）：「能读懂、不保证地道」——
  「干/乾/幹」「发/發/髮」这类一对多没有上下文，选的是常用形
- 无选区会提示先选中；ASCII、emoji、生僻字原样保留
- 想要词组级的地道转换得引入词组表 + 分词，那是另一个量级，暂不做

### 直接输出繁体（0.5.6）

想「打出来就是繁体」不用打完再全选转换：按 `Ctrl+Shift+T` 切换输出模式，HUD 显示「输出：繁体/简体」。

- 开启后**所有上屏文字**（候选、短语）自动过一遍简→繁映射；设置会记住（`defaults` 键 `OutputTrad`）
- 和 `Ctrl+Shift+F` 的分工：**F 是对已有文字原地转，T 是以后打的都转**

### WebDAV 同步：短语 + 重码记忆（0.5.8，0.5.9 配置可视化）

**同步什么**：`phrase.txt`（自定义短语）和 `freq.txt`（重码记忆）两个文件。
打错日志（mislog.tsv）不传。**怎么触发**：点菜单栏输入法名字弹出菜单，选
**「同步到网盘」** 或 **「从网盘恢复」**；快捷键 `Ctrl+Shift+U` / `Ctrl+Shift+D` 等价。

#### 首次配置：点「网盘配置…」就够了（0.5.9 起）

1. 点菜单栏输入法名字 → **「网盘配置…」**
   —— 第一次点会自动生成配置文件 `webdav.conf` 并用「文本编辑」打开。
2. 照文件里的注释填三行（把行首 `#` 去掉，右边填值），保存（⌘S）。
   文件位置：`~/Library/Application Support/SimpleFly/webdav.conf`。
3. 点菜单 **「同步到网盘」** —— HUD 显示「已同步 2 个文件」即配置成功，
   去网盘网页版能看到 `phrase.txt` 和 `freq.txt` 两个文件。

填好后的样子（以坚果云为例）：

```ini
url  = https://dav.jianguoyun.com/dav/SimpleFly
user = 你的坚果云登录邮箱
pass = 应用密码（第 2 步生成的，不是登录密码）
```

改完配置**不用重启输入法**——每次点同步时现读。

#### 坚果云详细步骤（配置文件注释的展开版）

| 步骤 | 操作 | 注意 |
|---|---|---|
| ① 建文件夹 | 浏览器打开 www.jianguoyun.com 登录 → 「我的坚果云」→ 新建文件夹 → 命名 `SimpleFly` | 名字随意但**必须英文**、不带空格；不要放进别的文件夹里 |
| ② 生成应用密码 | 右上角账户名 → **账户信息 → 安全选项 → 第三方应用管理 → 添加应用** → 输入名称（如 `xiaoman`）→ 系统生成一串随机密码 | **只显示这一次**，当场复制；忘了就删掉重新生成 |
| ③ 填 webdav.conf | `url` = `https://dav.jianguoyun.com/dav/SimpleFly`（= 固定前缀 + 第①步文件夹名）；`user` = 注册邮箱；`pass` = 第②步的应用密码 | 三行都填了才算配置完成 |

#### 其他支持 WebDAV 的网盘

配置方法完全一样，只是三个值不同：群晖/绿联 NAS（控制面板里开启 WebDAV 服务）、
InfiniCloud、Box 等。`user` / `pass` 用网盘提供的 WebDAV 专用凭据（很多网盘
WebDAV 密码和登录密码是分开的）。

#### 旧方式（defaults 命令行，仍然兼容）

```bash
D=com.simplefly.inputmethod.SimpleFly
defaults write $D WebDAVURL  "https://dav.jianguoyun.com/dav/SimpleFly"
defaults write $D WebDAVUser "你的坚果云邮箱"
defaults write $D WebDAVPass "应用密码"
killall SimpleFly && 切走再切回输入法
```

两种方式同时存在时，`webdav.conf` 里的键**逐项优先**（文件里填了 url，
user/pass 还会回落到 defaults）。

#### 出问题了对照查

| HUD 提示 | 原因 | 处理 |
|---|---|---|
| 401 认证失败 | `pass` 填成了登录密码 | 换成第②步生成的应用密码 |
| 404/409 目录不存在 | `url` 里的文件夹在网盘不存在；或 url 只写到 `/dav`（挂在根上写不进） | 补上文件夹名：`https://dav.jianguoyun.com/dav/SimpleFly` |
| 同步了但网盘空的 | 同步其实成功了，看错地方 | 网页版刷新「我的坚果云/SimpleFly」 |
| 恢复失败（本地未动） | 云端缺文件或网络问题 | 先「同步到网盘」一次再恢复；本地原样未被破坏 |

**两条语义**：

| 操作 | 行为 | 保护 |
|---|---|---|
| 同步到网盘 | 本地 → 云端覆盖（PUT） | 上传前先把重码记忆落盘，传的是最新数据 |
| 从网盘恢复 | 云端 → 本地覆盖 | **先全部拉到临时目录，两个文件都成功才动本地**；原件自动备份 `.bak`；任一失败本地原样不动 |

**隐私**：只传上述两个文件，存进你自己的网盘；应用密码明文存在本机
（`webdav.conf` / defaults），不出本机。
**边界**：手动同步、整体覆盖，无自动/双向合并——短语是单机手改的小文件，这个取舍够用。

---

## 六、自定义快捷输入

把常用长内容绑到一个短编码上。配置文件首次运行自动生成（带说明示例）：

```
~/Library/Application Support/SimpleFly/phrase.txt
```

```ini
# 一行一条，= 或 Tab 分隔，# 开头是注释
dz  = 我的常用地址
yx  = you@example.com
```

| 行为 | 说明 |
|---|---|
| 位置 | 敲完编码，自定义内容排候选窗**第一位**，空格或 `1` 上屏 |
| 优先级 | **高于码表**，但不顶掉码表原有候选，只是排最前 |
| 编码长度 | 任意长（可超 4 码、可含数字） |
| 合法字符 | `[a-z]`、数字，以及 `;` 和 `'`（它们是 speller 的正经码位，可以覆盖快符，如 `;a=自己常用的符号`） |
| 生效 | **改完文件立即生效**，不用重启（按 mtime 自动重载） |
| 不误触发 | 即使编码刚好 4 码（或刚好是 `;x` 快符），也**不会**被自动上屏顶掉，要按一下空格确认 |
| 静默跳过 | 编码里出现合法字符以外的东西，那一行会被**静默丢弃**（不报错） —— 发现短语不生效先查编码字符 |

---

## 七、配置项

域 = bundle id：`com.simplefly.inputmethod.SimpleFly`。改完 `killall SimpleFly` 重新加载。

```bash
D=com.simplefly.inputmethod.SimpleFly
defaults write $D EnableCompletion   -bool NO    # 关前缀补全（更像官方）
defaults write $D AutoCommit4        -bool NO    # 关自动上屏（四键 + ;x 快符），一律空格确认
defaults write $D MaxCandidates      -int 5      # 一页候选数（1–9，默认 9）
defaults write $D InlinePreedit       -bool NO   # 编码画在候选窗左侧（默认嵌在输入框里）
defaults write $D PunctAscii         -bool YES   # 启动即英文标点（等价 Ctrl+. 的历史态）
defaults write $D FullShape          -bool YES   # 启动即全角
defaults write $D InitialAsciiMode   -bool YES   # 启动即英文模式
defaults write $D ReverseModeAutoExit -bool YES # 查编码查一个字就退（默认 NO = 常驻）
defaults write $D Theme              -string dark # 候选窗配色主题（8 套，见 §四 Ctrl+;）
defaults write $D CodeHint           -bool YES   # 候选窗底部显示高亮候选的音形码（等价 Ctrl+Shift+H，即时生效）
defaults write $D PanelOffsetX       -float 12   # 候选窗水平微调（pt，正向右）
defaults write $D PanelOffsetY       -float -4   # 候选窗垂直微调（pt，正向上）
defaults write $D PanelFlipY         -bool YES   # 候选窗位置差得离谱时开：把光标矩形的 y 镜像一次
defaults write $D DebugPanelRect     -bool YES   # 定位诊断：把原始矩形打到系统日志（查完记得关）
```

| 键 | 默认 | 作用 |
|---|---|---|
| `EnableCompletion` | `YES` | 是否显示前缀补全候选（官方默认关，本版默认开，图省事） |
| `AutoCommit4` | `YES` | 自动上屏总开关，管两件事：四键唯一（`^\w{4}$`）+ `;x` 快符（`^;.$`）。关掉则一律空格确认 |
| `MaxCandidates` | `9` | 一页候选数（1–9） |
| `InlinePreedit` | `YES` | 编码嵌在输入框里；关掉则画在候选窗左侧 |
| `PunctAscii` | `NO` | 英文标点。`Ctrl+.` 会自动写回这一项 |
| `FullShape` | `NO` | 全角。`Shift+空格` 会自动写回这一项 |
| `InitialAsciiMode` | `NO` | 开机即英文模式 |
| `ReverseModeAutoExit` | `NO` | 查编码模式是否「查一个就退」 |
| `Theme` | `metro` | 候选窗配色主题。0.6.3 起内置 8 套：`metro` / `dark` / `paper` / `aqua` / `luna` / `ink` / `google` / `mojave_dark`，`Ctrl+;` 循环切换；写错名字自动退回 `metro` |
| `CodeHint` | `NO` | 候选窗底部显示**当前高亮候选**的音形码（`Ctrl+Shift+H` 同此开关）。开关实时读取，改完下一个候选窗即生效，**不用 killall** |
| `PanelOffsetX/Y` | `0` | 候选窗位置微调（pt）。**只救固定距离的偏** |
| `PanelFlipY` | `NO` | 把 IMK 返回的光标矩形在屏幕上做一次 y 镜像后再定位。定位「离谱」时用 |
| `DebugPanelRect` | `NO` | 每次定位把原始矩形 / 镜像结果 / 鼠标位置打到系统日志。查完记得关 |

> 单敲 `Shift` 的中英状态**刻意不落盘**：它是临时输入状态，不是偏好。开机总从中文开始，避免「怎么打不出汉字」。

---

## 八、码表：获取、安装、更换

### 先说结论

**本仓库不含码表文件**（小鹤音形码表版权归小鹤官方所有，不得再分发，详见同目录 `NOTICE`）。
码表拿到手后只是一个文件 `simplefly.dict`，放到下面任一位置即可：

| 位置 | 说明 |
|---|---|
| `~/Library/Application Support/SimpleFly/simplefly.dict` | **推荐**。运行时优先读这里，换表不用重新构建 |
| `resources/simplefly.dict`（仓库里） | 构建时打进 app bundle（已被 `.gitignore` 排除，不进版本控制） |

两个位置都没有会怎样：输入法能装、能选，但**按键不被拦截**（字母原样上屏），并在光标旁提示「未找到码表」—— **这是缺码表，不是装坏了**。

### 获取方式

**情况一：同一台机器升级（最常见）**

老码表就在 `~/Library/Application Support/SimpleFly/simplefly.dict`，新版本照常读取，**什么都不用做**。

**情况二：新机器 / 第一次构建**

码表文本版来自 [`cubercsl/rime-flypy`](https://github.com/cubercsl/rime-flypy) 仓库的 `flypy/` 目录（自述从小鹤官方 macOS 挂接包提取，与官方同版），三步生成：

```bash
# 1. 拉上游仓库（码表版权归小鹤官方，你只能自用，不要转传码表文件）
git clone https://github.com/cubercsl/rime-flypy.git /tmp/rime-flypy

# 2. 把官方 16 个分类合并成引擎用的单表 TSV
python3 tools/build_dict.py /tmp/rime-flypy/flypy \
        -o ~/Library/Application\ Support/SimpleFly/simplefly.dict

# 3. 重新加载
killall SimpleFly     # 然后切走再切回本输入法
```

生成结果约 **76,515 条 / 70,917 个编码**（首选/次选/三码填空/全码字词/快符/符号/表情等 16 类全在）。

> 官方网盘挂接包里的主码表是 **`.bin` 二进制**，本引擎只认上面的文本格式 —— 别直接拷 `.bin`。
> 官方仓库（[flypy.cc](https://flypy.cc)）的挂接包仍是合规的官方获取渠道；`rime-flypy` 只是把文本版整理好了。

### 换码表 / 更新版本

1. 用上面「情况二」的命令生成新的 `simplefly.dict`（覆盖旧文件即可）；
2. `killall SimpleFly`，切走再切回 —— 码表每次激活输入法时重新加载，**不用重新构建 app**。

### 码表格式（想手改 / 自制）

一行一条：`编码<TAB>词条`，**文件内顺序即优先级**（同编码多条时，靠前的排前面）。
字符集 `[a-z;']`，最长 4 码。改动会直接生效于下一次加载，没有校验工具 —— 改坏了表现为该码不出字。

---

## 九、已知限制

- **没有翻页**：一页最多 9 个候选（查编码模式按网格分页到 27/页），超出的不显示。
- **不支持鼠标点选候选**：只用键盘。
- **模式切换无菜单指示器**：切中/英、全角时只有 1 秒浮层提示，菜单图标不变（鼠须管会显示 `中`/`A`，本版未做）。
- **没有用户词库 / 自学习 / 调频**：码表只读，每次按键结果完全确定（与官方口径一致）。
- **没有简繁转换、`;f` 重复上屏、计算器、日期时间等 lua 功能**。
- **全角只作用于标点**：字母不进编码、数字仍是半角（与官方一致）。

---

## 十、故障排查（精简版）

### 换机器之后打不出字
按顺序查两件事：

1. **缺码表**（最常见）：看 `~/Library/Application Support/SimpleFly/simplefly.dict` 在不在，不在照 §八 生成。缺码表时按键不被拦截、光标旁有「未找到码表」提示。
2. **架构不对**：多半是**拷了已装好的 `.app` 而不是重编译**。照 §二 重新构建安装，然后用下面这条核对架构：

```bash
file ~/Library/Input\ Methods/SimpleFly.app/Contents/MacOS/SimpleFly
# 应当显示所在机器的架构（arm64 或 x86_64）；跟 CPU 对不上就一定跑不起来
```

### 系统设置里找不到这个输入法
通常是「装完没注销」或「首次添加没点允许」。先注销重登；仍不行用仓库自带的 `tools/tis_diag --select`（免注销直接切换输入源）验证进程和码表是否正常。

### 输入法列表里图标是一块纯色方块
历史上有两类成因（均已修，列出备查）：
- 菜单栏图标 PDF 必须**挖空**字形（模板图渲染只看 alpha，实心字会被涂成整块）。
- 应用图标 `.icns` 同样必须挖空（列表回退到 app 图标时也按模板图渲染）。
确认模式图标已修复（0.5.1 起）：

```bash
plutil -extract CFBundleVersion raw ~/Library/Input\ Methods/SimpleFly.app/Contents/Info.plist
# 应 ≥ 11（图标修复于 0.5.1）；比构建版本小 = 只编译没安装，重新跑 ./install.sh
```

### 设置列表里图标比别人小（26×26 小方块）

0.5.1 已修好并**真机验证**：模式图标从 22×16 pt 的 PDF 换成 Apple 同款 **双帧 TIFF**
（`SimpleFly.tiff`，16×16@72dpi + 32×32@144dpi），三个 `tsInputMode*IconFileKey` 指向它。
**根因**：macOS 26 的设置列表不采用 PDF 模式图标，一直回退到 `.icns` 的 16×16 帧（26×26 px）；
PDF 在菜单栏能用，不等于设置列表会用。

自查顺序：

```bash
# 1. 装的是不是 0.5.1 以上
plutil -extract CFBundleVersion raw ~/Library/Input\ Methods/SimpleFly.app/Contents/Info.plist   # 应 ≥ 11
# 2. 模式图标是不是指向 TIFF
plutil -p ~/Library/Input\ Methods/SimpleFly.app/Contents/Info.plist | grep tsInputModeMenuIcon   # 应为 SimpleFly.tiff
# 3. 图标文件在不在、几个帧
sips -g pixelWidth -g pixelHeight ~/Library/Input\ Methods/SimpleFly.app/Contents/Resources/SimpleFly.tiff
```

改完要**注销重登**才会刷新（TIS 按会话缓存 bundle 元数据）；只用 `killall TextInputMenuAgent SystemUIServer`
只能刷菜单栏。仍不行可试装到 `/Library/Input Methods/`（需 sudo）。

---

## 十一、卸载

```bash
./uninstall.sh
```
